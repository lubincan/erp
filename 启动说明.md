# 项目启动说明

## ✅ Conda环境已配置完成

- **环境名称**: erp
- **环境路径**: C:\Users\Lucas\.conda\envs\erp
- **Python版本**: 3.10
- **依赖状态**: ✅ 已安装

## 🚀 启动项目的方法

### 方法1：使用启动脚本（推荐）

**Windows系统：**
```bash
# 双击运行
start_conda.bat

# 或在命令行中
start_conda.bat
```

### 方法2：手动启动

**在命令行中执行：**

```bash
# 1. 激活conda环境
conda activate erp

# 2. 启动项目
python app.py
```

**或直接使用完整路径：**
```bash
C:/Users/Lucas/.conda/envs/erp/python.exe app.py
```

### 方法3：在VSCode中启动

1. 打开终端（Ctrl + `）
2. 选择conda环境：`erp`
3. 运行：`python app.py`

## 📍 访问地址

启动成功后，在浏览器中访问：

- **首页**: http://127.0.0.1:5000
- **打卡页面**: http://127.0.0.1:5000/attendance

## 🔍 验证启动成功

启动成功后，你会看到类似以下输出：
```
==================================================
ERP系统启动中...
访问地址: http://127.0.0.1:5000
默认管理员: admin / admin123
==================================================
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

## ⚠️ 如果遇到问题

### 端口被占用
如果5000端口被占用，修改 `app.py` 中的端口：
```python
app.run(host='0.0.0.0', port=8080, debug=True)  # 改为8080
```

### 环境未激活
确保使用conda环境的Python：
```bash
which python  # 应该指向conda环境的python
```

### 依赖缺失
如果提示缺少模块，重新安装：
```bash
conda activate erp
pip install -r requirements.txt
```

## 📝 常用命令

```bash
# 查看conda环境
conda env list

# 激活环境
conda activate erp

# 退出环境
conda deactivate

# 查看已安装的包
conda list
# 或
pip list
```

